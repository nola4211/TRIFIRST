# TriFirst Frontend

## Run locally

1. Install dependencies:

   ```bash
   npm install
   ```

2. Start dev server:

   ```bash
   npm run dev
   ```

The frontend runs on Vite and proxies `/api/*` to `http://localhost:8000`.
